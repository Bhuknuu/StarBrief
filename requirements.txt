# StarBrief — Python Dependencies
# Core HTTP & Scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Fuzzy Matching (Deduplication)
thefuzz[speedup]>=0.22.0

# Database (Neon PostgreSQL)
psycopg2-binary>=2.9.0
asyncpg>=0.29.0

# LLM (Gemini API)
google-generativeai>=0.8.0

# Data Validation & Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Retry / Resilience & Routing
tenacity>=8.2.0
litellm>=1.35.0

# --- Optional ---
# playwright>=1.42.0    # only for JS-heavy sites (Luma, etc.)
